#include <iostream>
using namespace std;

int main() {
    int s[3], lv[3], st[3];
    for (int i = 0; i < 3; i++) {
        cin >> s[i] >> lv[i] >> st[i];
    }
    int a, b;
    cin >> a >> b;

    int total_area = 4 * a * b;

    int min_price = 1e9; // много голяма стойност

    for (int i = 0; i < 3; i++) {
        int boxes = (total_area + s[i] - 1) / s[i];  // закръгляне нагоре
        int price_in_stotinki = boxes * (lv[i] * 100 + st[i]);

        if (price_in_stotinki < min_price) {
            min_price = price_in_stotinki;
        }
    }

    // Преобразуваме обратно в левове и стотинки
    cout << min_price / 100 << " " << min_price % 100 << "\n";

    return 0;
}
